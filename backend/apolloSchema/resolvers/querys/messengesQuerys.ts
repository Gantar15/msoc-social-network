import { IApolloContext } from "types/IApolloContext";

export default {
    async getMessenges(_: any, {recipientId, messenge}: {recipientId: number, messenge: string}, {req}: IApolloContext){
        
    }
};